# JSON 數據管理器測試指南

## 快速開始

### 1. 使用快速測試腳本

最簡單的方法是使用 `quick_test.py`：

```bash
# 方法一：直接運行，然後輸入IP
python quick_test.py

# 方法二：命令行直接指定IP
python quick_test.py emulator-5554
python quick_test.py 192.168.1.100
python quick_test.py adb-fc65396d-4LPqmI
```

### 2. 使用完整測試腳本

使用 `test_json_manager.py` 進行完整測試：

```bash
python test_json_manager.py
```

然後選擇測試選項：
- 選項 1：預設測試
- 選項 2：互動測試（輸入您的IP）
- 選項 3-6：特定功能測試

## 常用的設備 IP 格式

根據您的代碼，常見的設備 IP 格式包括：

```
emulator-5554
emulator-5556
emulator-5558
emulator-5560
emulator-5562
192.168.31.119
192.168.31.204
192.168.50.127
3a8d31f2
7fe98fc6
fc65396d
adb-fc65396d-4LPqmI
adb-fc65396d-4LPqmI (2)._adb-tls-connect._tcp
```

## 功能說明

### 停車場數據管理器 (ParkMarketDataManager)

```python
# 創建管理器
park_mgr = json_manager.create_park_manager("您的設備IP")

# 檢查是否需要購買
should_buy_daily = park_mgr.should_purchase('daily', max_purchases=2)
should_buy_weekly = park_mgr.should_purchase('weekly', max_purchases=2)

# 記錄購買
park_mgr.record_purchase('daily', buy_num=1, check_time=1)
park_mgr.record_purchase('weekly', buy_num=1, check_time=1)

# 獲取數據
daily_ts, daily_num, weekly_ts, weekly_num = park_mgr.get_buy_data()
```

### 時間記錄管理器 (TimeRecordDataManager)

```python
# 創建管理器
time_mgr = json_manager.create_time_manager("您的設備IP")

# 記錄時間
time_mgr.record_time("park")  # 停車
time_mgr.record_time("battle")  # 戰鬥

# 檢查記錄
record = time_mgr.get_time_record("park")
is_same_day = time_mgr.is_same_day("park")
is_expired = time_mgr.is_expired("park", expired_seconds=3600)
```

### 商店數據管理器 (StoreDataManager)

```python
# 創建管理器
store_mgr = json_manager.create_store_manager("您的設備IP")

# 記錄購買
purchase_data = {
    "quantity": 5,
    "last_time": "2025-09-30 15:30:00",
    "price": 100
}
store_mgr.record_purchase("商品名稱", purchase_data)

# 檢查記錄
is_purchased_today = store_mgr.is_purchased_today("商品名稱")
is_expired = store_mgr.is_purchase_expired("商品名稱", hours=24)
```

## 生成的文件說明

測試會在當前目錄生成以下文件：

1. `{設備IP}.json` - 停車場數據
2. `{設備IP}_test.json` - 時間記錄數據  
3. `{設備IP}_test123.json` - 商店購買數據

例如，如果您的設備IP是 `emulator-5554`，會生成：
- `emulator-5554.json`
- `emulator-5554_test.json`  
- `emulator-5554_test123.json`

## 向後兼容

如果您有舊的代碼，可以直接使用這些函數：

```python
# 舊的函數仍然可用
json_manager.time_recording("設備IP", "動作名稱")
record = json_manager.return_time("設備IP", "動作名稱") 
json_manager.record_json("設備IP", "標題", 內容字典)
result = json_manager.check_json("設備IP", "標題")
```

## 台灣時區

所有時間操作都使用台灣時區 (UTC+8)，確保時間記錄的準確性。

## 錯誤處理

- JSON 文件損壞時會自動創建備份
- 數據類型錯誤會自動修正為默認值
- 舊數據格式會自動兼容和升級

## 示例輸出

運行測試後您會看到類似這樣的輸出：

```
快速測試設備: emulator-5554
========================================

1. 停車場數據:
   當前JSON數據:
   {
       "daily": {
           "car_market_timestamp": 1727688000.0,
           "car_market_buy_num": 1
       },
       "weekly": {
           "car_market_timestamp": 1727688000.0, 
           "car_market_buy_num": 1
       }
   }
   Daily: 次數=1, 需要購買=True
   Weekly: 次數=1, 需要購買=True

2. 時間記錄:
   快速測試記錄: {'timestamp': 1727688123.45, 'date': '2025-09-30', 'datetime': '2025-09-30 15:35:23'}

3. 商店記錄:
   快速測試購買記錄: {'last_time': '2025-09-30 15:35:23', 'test': True}

✅ 設備 emulator-5554 測試完成!
```
