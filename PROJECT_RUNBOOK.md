# 專案執行與維護指引

更新日期：2026-03-12

## 1. 啟動概念

- 以 `new_main_v2.py` 作為主要啟動入口。
- 啟動後會進行：裝置連線 -> 截圖/辨識 -> 任務執行 -> 日誌記錄。

## 2. OCR 與伺服器

- OCR 可配置主備伺服器（例如 `100.64.0.5` 與 `100.64.0.7`）。
- 當主站超時可切到備援，並建議搭配健康檢查機制。
- HTML 面板可顯示目前實際使用中的 OCR 位址，避免誤判「看似失敗」狀況。

## 3. RL Logs 目前規範

- 已統一路徑為 `miner/rl_logs`。
- 不再使用 `miner/rl/rl_logs`。
- 已加入 `events.jsonl` 輪替（rotate）避免檔案過大。

## 4. 風險最低的優化方向

- 優先做可觀測性（狀態顯示、錯誤訊息精準化）
- 優先做路徑與設定一致化
- 優先做健康檢查與超時策略
- 保守調整辨識閾值，並保留回滾能力

## 5. 維護原則

- 這是腳本型專案：每次改動都要可追蹤、可回滾。
- 截圖資產視為必要依賴：刪除前必須先檢查引用與回歸。
- 關鍵檢查（如 `get_stage_with_check`）屬必要即時驗證，不以快取取代。
- 每個截圖檢查皆屬必要流程：每次都要即時檢查，不能靠快取跳過。
