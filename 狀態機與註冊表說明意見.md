# 狀態機與註冊表說明意見

> 目的：回答「程式碼規模已大到無法靠單人心智處理，是否該導入正式狀態機（FSM）？
> 還是該如何降低複雜度？」
>
> 結論先行：**不該導入正式狀態機**。本系統的複雜度來源不是「狀態組合失控」，
> 而是「同一份任務知識被分散在四、五張平行表格」，以及 repo 衛生問題。
> 正確解方是 **Task Registry（任務註冊表，資料驅動）** 與 **清理工作流**。
>
> 本文附具體檔案/行號證據，供與他人論證時引用。所有數字以
> 2026-08-09 的 `main` 分支為準。

---

## 1. 背景與現況盤點

主樹（排除 `.conda`、`OCR/PaddleOCR` 等 vendored 依賴、worktree）規模：

| 指標 | 數值 |
|---|---|
| .py 檔案數 | 989 |
| 總行數 | 約 16.4 萬行 |
| 核心大檔 | `ws_token/runner.py` 2130 行、`miner/mining_service.py` 1504 行、`ws_token/mining_supervised.py` 1347 行、`device_wrapper.py` 1352 行、`config_manager.py` 1201 行 |

系統本質：**多裝置批次掛機 bot**。每台裝置由 `new_main_v2.py` 起執行緒，
跑「休眠 → 喚醒 → 連線 → WS 階段 → 瀏覽器/ADB 階段 → 休眠」的輪詢迴圈。
後端有兩套：純 WS（websocket 協議直連）與視覺（ADB/Playwright），
且採「WS-first」策略：先跑 WS 階段，成功者由對照表標記讓瀏覽器階段跳過。

---

## 2. 問題診斷：複雜度從哪裡來

### 2.1 不是控制流複雜，是「跨層平行表格」

要回答「某個任務今天到底跑不跑、跑在哪、跑完記錄到哪」，需要同時查以下分散各處的資料：

| 表 | 位置 | 內容 |
|---|---|---|
| 任務順序表 | `ws_token/runner.py:90` `TASK_ORDER` | 41 個任務的線性順序 |
| WS→pipeline skip 對照 | `game_actions/ws_phase.py:28` `WS_TO_PIPELINE_SKIPS` | WS 任務成功 → 瀏覽器階段可跳過哪些任務 |
| skip→dashboard 徽章對照 | `game_actions/ws_phase.py:56` `SKIP_TO_DAILY_RECORD` | 跳過的任務要回寫哪些 JSON key 讓徽章亮起 |
| 特例 schema 回寫 | `game_actions/ws_phase.py:98-123` | `mission_timestamp` 是 flat scalar、`farm_plant_click` 是 dict，各自獨立處理 |
| config 旗標 | `config_manager.py` 36 個（`open_lamp`、`couple_gifts`、`forge_ring`、`workshop_rotate`、`ws_token_spend`、`enable_*`…） | 每個任務的啟用/花費/行為開關 |
| scheduler 三兄弟 | `game_actions/` 下 8 個檔案：`carpark_scheduler.py`、`dragon_realm_scheduler.py`、`dungeon_scheduler.py`、`escort_scheduler.py`、`fannaoxiao_scheduler.py`、`lamp_scheduler.py`、`mail_scheduler.py`、`redpack_scheduler.py` | 每個都是 `_is_enabled / _is_due / _mark_done` 的複製貼上模式 |

**新增一個遊戲任務的實際成本**：WS 實作 + Playwright 實作 + skip 對照 +
徽章回寫 + config 旗標 + scheduler + 測試。這是**乘法複雜度**——
知識不是長在一個地方，而是七個地方都要同步改，漏一處就是靜默 bug。

### 2.2 症狀證據

- `runner.py` 的任務門檻高度客製：開神燈要批次上限（`_LAMP_BATCH_NUM`）、
  萬神試煉要短探測 timeout（`_ROGUE_PROBE_S`）、尋寶要 6 秒探測（`_TREASURE_PROBE_S`）、
  加工坊要 12h 輪換計時。這些「任務特例」全部塞在同一個線性函式裡，
  每個任務實際是「帶特例的微狀態機」，卻沒有把它們形式化。
- `runner.py` 從歷史 sync-conflict 檔可看出 2026-06 為 1586 行 → 2026-08 為 2130 行，
  一個月長 500 行，且沒有收斂跡象。
- 測試檔 `tests/test_ws_token_runner.py` 已 1729 行——這其實是好消息：
  它證明測試是控複雜度的有效槓桿，問題是要控的目標是「表格」而不是「狀態」。

---

## 3. 論點一：不該導入正式狀態機

### 3.1 系統本質是批次輪詢，不是事件驅動

狀態機的價值在「事件來得不可預期、狀態組合多到無法用 if-else 窮舉」。
本系統相反：每小時定時喚醒，跑完固定任務清單就睡，事件源只有少數幾個
（強制休眠、手動接管、登入衝突）。**狀態組合是有限且可窮舉的**，
`if/else` 完全負擔得起，導入 FSM 只是把 if-else 換成查表儀式，沒有減少知識量。

### 3.2 現有訊號機制已是足夠的逃生閥

`bot_state.py:12` 已有 `Signal(Enum)`（SKIP_SLEEP / FORCE_SLEEP / WEB_CLOSE /
MANUAL_RELEASE），加上 pause、force_sleep、web_launch、manual_release 等一組
request/consume 函式。裝置層的「狀態」也已存在：`status` 字串
（ONLINE/OFFLINE/PAUSED/ERROR）＋ `task`/`step` 描述（`bot_state.py:46`）。
也就是說，**該有的狀態追蹤已經有了**，缺的不是形式化，而是單一真相來源。

### 3.3 `.planning/analysis/FIRST_STEP.md` 的 FSM 提案是泛用模板，不可直接採納

該檔的 DeviceStateMachine 提案（IDLE/RUNNING/RECOVERING/PAUSED/ERROR）有以下問題：

- 它是 AI 規劃工具產出的泛用骨架，其「Roadmap Status」表與實際程式碼不符
  （例如 Phase 4 的雙週副本早已完成，但表上同時出現多個互相矛盾的狀態）。
- 它假設狀態需要 timeout 自動恢復（RECOVERING），但本系統的恢復邏輯
  （`runtime_services/` 的 device_runtime、sleep_service、ws_fallback）已經各自實作，
  且互相耦合在「喚醒週期」上，強行收進 5 個狀態會把現有邏輯打掉重寫。
- 它把「顯示狀態」（dashboard 要看的）與「執行狀態」（程式內部判斷的）混為一談，
  前者只需唯讀快照，後者才是狀態機，兩者需求不同。

### 3.4 什麼狀況才值得導入狀態機（保留彈性）

唯一有潛力的點是**裝置生命週期**（offline/connecting/ws_phase/browser_phase/
manual_mode/sleeping/force_sleep）。若未來出現「從任意狀態可跳到任意狀態、
轉換規則互斥衝突」的實際 bug，才需要把它形式化。目前轉換規則靠
bot_state 的訊號＋各 service 的迴圈處理，邊際收益很低，列為「觀察清單」即可。

---

## 4. 論點二：Task Registry 才是解法

### 4.1 核心思想：單一真相來源

把第 2 節那五張平行表格**併成一張**。每個任務是一個 dataclass：

```python
@dataclass(frozen=True)
class TaskDef:
    name: str                 # "lamp" / "開神燈"
    order: int                # 取代 TASK_ORDER 的線性順序
    enabled_key: str | None   # config 旗標，None = 永遠開
    ws: Callable | None       # WS 後端實作
    browser: Callable | None  # Playwright/ADB 後端實作
    skip_browser_keys: tuple[str, ...] = ()
    dashboard_record_keys: tuple[str, ...] = ()
    record_schema: str = "time_recording"  # 特例: "flat_scalar" / "farm_dict"
```

### 4.2 收益

| 項目 | 現在 | Registry 後 |
|---|---|---|
| 新增任務 | 改 7 個地方 | 加一列資料 |
| `runner.py` | 2130 行（含全部特例） | 資料表 + ~600 行通用執行骨架 |
| skip 對照 | ws_phase.py 兩張 dict + 兩個特例函式 | 任務自身的欄位 |
| 測試 | 手寫每個任務的邏輯測試 | table-driven：新增任務自動被通用測試覆蓋 |
| 任務特例 | 散在 runner 各函式的常數與分支 | 資料表欄位（`batch_cap`、`probe_timeout`…）|

### 4.3 現有模式已經驗證了方向

`ws_token/carpark_plan.py` 的 `parse_plan` 與 `ClusterScanConfig` 已是
「設定資料化」的成功先例；`tests/test_ws_token_runner.py` 1729 行證明
每個任務可被單獨測試。Registry 是把這個模式推廣到全部 41 個任務。

---

## 5. 論點三：repo 衛生是免費的勝利

論證前必查的數字（這影響所有 AI 協作的語境品質）：

- 主樹混入 **sync-conflict 殘骸**：`ws_token/lamp.py`、`online_monitor.py`、
  `mining_adapter.py`、`equipment_scheme.py`、`ws_state/*.json` 都有
  `.sync-conflict-*.APSLDIE` 副本。這些是 NAS 同步打架產生的垃圾，
  每次 grep/import 導航都被污染，且內容可能是舊版程式碼（誤讀風險）。
- 存在大量 stale worktree：`.worktrees/`、`worktree/`、`.claude/worktrees/`
  數十個，與 AGENTS.md「合併後清理」規範不符；裡面還有 2026-07 的
  sync-conflict 檔與 `.pytest_cache` 權限錯誤。
- 未提交垃圾：`NUL`、`_check_devices.py`、`emu-test.json`、`web-002/003/004.json`。
- `ws_state/` 是執行期狀態，不該進 git。

清理這些**不動任何邏輯**，卻讓每次程式閱讀與 AI 協作的噪音大幅下降，
是最低風險、最高性價比的一步。

---

## 6. 實施路徑（若決定動手）

1. **先清衛生**（一天內）：刪 sync-conflict、清 stale worktree、
   移除未提交垃圾、`ws_state/` 進 .gitignore。
2. **設計 TaskDef 資料表**（兩天內，不改行為）：先只做「資料表 + 讀取層」，
   把 `TASK_ORDER` 與兩張對照表併入，執行層不動，跑既有測試證明無行為變化。
3. **逐步遷移**：一次遷移一個任務的執行層到通用骨架，每步跑
   `tests/test_ws_token_runner.py` 與目標測試（AGENTS.md 規範）。
4. **新增任務改走資料表**：在 AGENTS.md 補一條「新任務必須以 TaskDef 註冊」。

風險控制：全程 worktree 隔離、每步可回滾、只搬移知識不改語義。
最大風險是遷移中途又併入新任務（可先凍結新功能一週）。

---

## 7. 反方質疑與回應

| 質疑 | 回應 |
|---|---|
| 「狀態機比較正式，遲早要用」 | 正式 ≠ 正確。先讓資料單一化，若裝置生命週期日後真的爆出轉換衝突，再對那層上狀態機；任務層永遠不需要。 |
| 「Registry 只是把 if-else 搬到資料，沒省程式碼」 | 省的不是字數，是**同步成本**：新增/修改任務不再需要同時改 7 個檔案，漏改造成的靜默 bug 是最大成本。 |
| 「40 個任務一次遷移太大」 | 分步遷移，每步獨立驗證；先遷 3 個代表性任務（有 skip 的、有特例的、純 WS 的）建立模板。 |
| 「WS 與 Playwright 遲早合一，現在別投資」 | 正是合一的手段：registry 把兩者並列為同一 TaskDef 的兩個實作，skip 邏輯內建，ws_phase 的對照 patchwork 消失。 |
| 「現況能跑就不要動」 | 能跑與能改是兩回事。本文件 2.2 節顯示規模每個月 +500 行且無收斂，現在不動，三個月後遷移成本更高。 |

---

## 8. 結論

1. **不導入正式狀態機**：系統是批次輪詢，狀態有限，現有 Signal + status 已夠用；
   `.planning` 的 FSM 提案是泛用模板，採納它只會多一層儀式並打掉現有恢復邏輯。
2. **導入 Task Registry**：把分散在 5 張平行表格的任務知識併成單一資料表，
   讓新增任務從「改 7 個地方」變成「加一列資料」，這是複雜度的根治點。
3. **先清 repo 衛生**：sync-conflict 殘骸、stale worktree、未提交垃圾，
   零風險、立即見效，是所有後續改動的乾淨基底。

> 一句話：**這專案需要的不是狀態機，是任務註冊表。**
