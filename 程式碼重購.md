# 程式碼複雜度降低論點：不需要狀態機，需要表格驅動的任務註冊表

> 撰寫日期：2026-08-09
> 觸發問題：「程式碼太多太大量，純靠自己處理已無法應付，是否該設計狀態機？」
> 範圍：`game_actions/daily_pipeline.py` 為核心，連帶 `ws_token/runner.py`、`game_actions/task_due.py`、`utils/page_detector.py`、`task_sandbox/`。
> 與既有文件關係：本文回答「架構層該往哪走」；`docs/REFACTORING_OPPORTUNITIES.md` 是跨子系統待辦總表，`docs/REFACTOR_STATE_MANAGEMENT.md` 是 `bot_state` 的分階段展開。三者互補不重疊。

---

## 0. TL;DR

**結論：不需要狀態機。** 瓶頸不是狀態轉移，而是**任務中介資料（metadata）被寫成了程式碼**。

正確的下一步是把 `daily_pipeline` 從命令式序列改成**宣告式任務註冊表**，形狀直接沿用 `ws_token/runner.py` 已驗證可行的 `TASK_ORDER` + registry 分派 + `RunReport`。

狀態機有它該去的地方（任務**內部**的 UI 導航），但那裡的正確做法是先接單一 live 消費者，不是先建框架（原因見 §4）。

---

## 1. 診斷：三個證據

### 1.1 頂層流程本來就是線性的

`SCAN → WAKE_UP → CHECK_STAGE → 任務序列 → SLEEP`，沒有分支圖。

狀態機的價值在於「多狀態 x 多合法轉移，而你常掉進非法轉移」。目前唯一的非法狀態是「不在主頁面」，而它**已經有守衛**：

- `daily_pipeline.py:205-223` `_track()`：連續 4 次不在主頁面就強制關 app 並丟 `_ConsecutiveMismatchAbort`
- `daily_pipeline.py:225-229` `_guarded_run()` / `stage_guard._run_at_main_page`：每個任務進場前的主頁守衛

在這之上再套 FSM 只是換個寫法，不會減少任何需要記在腦中的東西。

### 1.2 真正的痛點：約 30 個任務各自重抄同 6 件事

`daily_pipeline.py` 共 557 行，其中 `_run_tasks`（`:170-557`）一個函式約 388 行。

每個任務內聯重複同樣 6 個關切：

| # | 重複的關切 | 呼叫形式 |
|---|---|---|
| 1 | force-sleep 中斷點 | `_force_sleep_checkpoint()` |
| 2 | WS 階段已完成則跳過 | `if not _ws_skip("任務名")` |
| 3 | due 判斷 | `task_due.is_due(...)` / `return_time` + `is_next_day` |
| 4 | 主頁守衛 | `_guarded_run(...)` / `_run_at_main_page(...)` |
| 5 | 狀態回報 | `bot_state.update_state(ip, task=..., step=...)` |
| 6 | 執行紀錄 | `time_recording(ip, name=...)` |

**客觀信號：任務編號已經破表。** 現行序列的註解編號是

```
Task 0 / 0.1 / 0.5 / 1 / 2 / ... / 13 / 13.5 / 14 / 14.5 / 14.6 / 14.65 / 14.7 / 15 / ... / 20
```

要插新任務只能挑一個小數（已經出現到 `14.65`）。這是清單超出原設計的鐵證。
同時 `_run_tasks` docstring 仍寫「20 tasks」，實際已約 29 項 — 文件漂移也是同一個成因。

### 1.3 你已經有正確答案，只是只用在一半

`ws_token/runner.py:90-98` 的 `TASK_ORDER` 是宣告式的 40 個任務名 tuple，配 registry 分派、per-task try/except、frozen `RunReport`（`:115-149`）。這是對的形狀，而且已經在跑。

`game_actions/task_due.py` 的 docstring 自己寫著：

> 「Phase A：把散在各 execute 函式內的 due 判斷收斂到這裡的純 predicate。」

也就是說**宣告化已經開工**，但只收斂了「due」這一個維度，其餘 5 個維度還留在 `_run_tasks` 裡內聯。

而 `DailyContext.ws_done: frozenset`（`daily_pipeline.py:119`）這個橋接欄位本身就是症狀：**同一份任務清單維護了兩次** — WS 一份順序、pipeline 一份順序，靠字串比對對齊。兩邊對不上會靜默漏做任務，且目前無測試保護。

---

## 2. 建議做法：表格驅動的任務註冊表

把 `daily_pipeline` 從命令式序列改成宣告表：

```python
@dataclass(frozen=True)
class TaskSpec:
    name: str                    # "地獄之門"，同時是 ws_done 的 key
    order: float                 # 取代 13.5 / 14.65 這種註解編號
    run: Callable[[DailyContext], None]
    due: str | None = None       # task_due registry key；None = 每輪都跑
    flag: str | None = None      # 對應的 config 開關（enable_hellgate 等）
    needs_main_page: bool = True
    record: str | None = None    # time_recording 名稱
    backends: frozenset = frozenset({"adb", "web_h5"})
```

`_run_tasks` 收縮成約 40 行的單一迴圈：依 `order` 排序，6 個共用關切在迴圈裡各做一次。

### 收益（複合）

- **新增任務 = 加一列表格**，不是在 388 行函式裡找位置插小數編號。
- **WS-skip 從約 20 處散落的 `if not _ws_skip(...)` 變成兩張表的 join**；順序或名稱不一致會在測試期就爆，而非 live 靜默漏做。
- **這張表可直接餵儀表板**：本輪哪些任務跑了、哪些被哪個條件（flag / due / backend / ws_done）跳過。目前要回答這個問題只能 grep log。
- **pipeline 側也能產出 `RunReport`**，與 WS 側同型，兩條路徑的可觀測性統一。

### 風險控制

1. 先寫**特徵化測試**釘住現行約 30 個任務的順序與 gating 條件。
2. 再搬 — 搬的過程是**純 code motion，行為零變更**。
3. 打法與 `docs/REFACTOR_STATE_MANAGEMENT.md` 的 Phase 0 相同（先安全網、後結構）。
4. `_run_tasks` 是 live bot 熱路徑；改完必須重啟 `new_main_v2.py`（`sys.modules` cache，改了不重啟等於白改）。

### 必須保留的隱性契約（搬動時最容易漏）

- Task 4（家族任務）取得的 `stage` 被 Task 5、6（守護靈 / 技能夥伴）復用。
- Task 18（好友每日禮物）執行後刷新 `stage`，供 Task 19（開神燈）使用。
- Task 12（所有日常任務）限 20:00–23:00 才跑。
- Task 1（地獄之門）刻意使用 pipeline 開頭捕捉的 `current_time`，**不是**即時 `now`（避免跨時段邊界漏做）。
- 尾端裝置專屬清理分支：`emulator-5558` 切「騙人用」方案、`fc65396d` 先開 Chrome 再關 app。
- `_DEVICE_SKIP_GUARDIAN`（`emulator-5558` 跳過守護靈 / 技能夥伴）。

---

## 3. 狀態機真正該用在哪

值得注意的是任務**內部**的 UI 導航：「我現在在哪個頁面、要怎麼走到目標頁」 — 那才是真的圖結構。

而這裡已有兩份現成資產：

- `utils/page_detector.py` 的 `PageState` enum（cocos-first / OCR-fallback）
- `task_sandbox/` 的 `NavTarget`

**但不要先建框架。** 挑一個任務（建議神燈 — `task_sandbox` 已經拿它當第一個實作），只為它接上 NavTarget，跑穩了再擴。

---

## 4. 反模式警告：本 repo 的失敗模式是「先建框架後找消費者」

依 `CLAUDE.md` 與 `docs/INDEX.md` 記載，這個 repo 建過的抽象層：

| 抽象 | 現況 |
|---|---|
| `farm_v2/states.py` 狀態機 | 2026-07-05 移除，**從未接線** |
| `task_sandbox/` | experimental，**未接主迴圈**（live 神燈仍走 `lamp_scheduler` → `LampService`） |
| `utils/page_detector.py` | **flag-gated experimental** |
| `bootstrap/api_services.py` | 已抽出，**未接線**（與 `new_main_v2.__main__` 重複） |

四個抽象層，四個沒進 live path。

**因此立規則：任何新抽象層，唯一消費者必須是 live 路徑，否則就是第五個死抽象。** 這也是為什麼 §2 選擇「改造既有的 `daily_pipeline`」而不是「新建 orchestrator 框架」。

---

## 5. 其他降複雜度的槓桿（按 ROI 排）

### 5.1 三後端平行實作是體積的真正乘數（最大槓桿）

同一個遊戲任務目前有 adb / h5 / ws 三份實作。`daily_pipeline._sea_dispatch`（`:76-85`）就是這個路由的縮影：H5 走 `sea_v2`、adb 走 legacy `Sea.sea`，外加 `use_sea_v2` flag。

收斂到「**WS 優先，UI 當 fallback**」能砍掉的行數遠超任何局部重構。這也是為什麼 §2 的 `TaskSpec` 要有 `backends` 欄位 — 它是這次收斂的登記處。

### 5.2 每輪產出結構化 RunReport 上儀表板

把「它到底做了沒」從**讀 log** 變成**看表**。這直接減少需要放在腦中的量，是「無法應付」的直接解藥；也讓 AI / subagent 協作時有客觀的驗收面。

### 5.3 局部整潔（優先序在前兩項之後）

`docs/REFACTORING_OPPORTUNITIES.md` 仍開著的複雜度項：cx-2（carpark 內嵌 JS walk + worldToScreen）、cx-4（`park_one_silver` 深巢 + 複製貼上回復塊）、cx-5（`_start` 150 行）、cx-7（`reconcile()` 131 行）。

這些是局部整潔，**不是**目前壓力的來源。等表格驅動落地後再處理。

---

## 6. 明確不做的事（YAGNI 護欄）

- **不**為頂層流程引入 FSM library / actor framework / asyncio 重寫。
- **不**新建 orchestrator 框架 — 改造既有 `daily_pipeline`，唯一消費者是 live 路徑。
- **不**在同一輪同時動 UI 導航狀態機與任務註冊表；兩者獨立、各自一輪。
- **不**在特徵化測試就位前搬動 `_run_tasks`。

