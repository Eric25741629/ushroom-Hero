# 菇勇者自動化 - 跨界車位搶占功能指南 (Fight Car Guide)

本文檔總結了 `fight_car_task.py` 腳本的開發心得、操作邏輯與 Web 介面的整合功能。

## 1. 核心腳本 (Script)

**檔案名稱**: `fight_car_task.py`

### 1.1 功能概述
此腳本專為「跨界車位」設計，能夠自動定位伺服器與車位，並在指定時間進行精準的搶占操作。支援時間偏移以應對遊戲伺服器延遲。

### 1.2 使用指令

在終端機 (Terminal) 執行：

```powershell
# 基本格式
python fight_car_task.py <伺服器>-<車位>+<結束時間>

# 範例：搶占 S1470 的 7 號車位，時間 14:30:00
python fight_car_task.py s1470-7+14:30:00
```

### 1.3 進階參數：時間偏移 (Time Offset)
若發現遊戲伺服器時間較慢或有延遲，可加入偏移秒數進行修正。

```powershell
# 範例：目標時間 + 1.5 秒 (晚 1.5 秒執行)
python fight_car_task.py s1470-7+14:30:00 +1.5

# 範例：目標時間 - 0.5 秒 (提早 0.5 秒執行)
python fight_car_task.py s1470-7+14:30:00 -0.5
```

---

## 2. 執行邏輯與機制 (Execution Logic)

腳本執行過程分為以下幾個階段，確保穩定性與速度：

### 階段一：自動搜尋與定位
1.  **伺服器定位**: 自動辨識畫面中的伺服器 ID 並點擊。
2.  **車位搜尋 (容錯機制)**:
    *   首先在當前畫面搜尋目標車位 (OCR)。
    *   若未找到，自動執行「下滑 (Swipe)」並點擊特定座標重置畫面。
    *   進行第二次搜尋。若兩次皆失敗則報錯。

### 階段二：提早部署 (Early Arrival)
*   **觸發時間**: 目標時間前 **2 分鐘** (T-120s)。
*   **動作**: 點擊車位進入詳情，並點擊搶占按鈕 (座標 198, 790) 進入準備畫面。
*   **目的**: 避免時間到時因網路或轉場延遲錯失良機。

### 階段三：精準搶占 (Sniping)
*   **觸發時間**: 目標時間(含偏移)前 **3 秒** (T-3s)。
*   **動作**: 啟動高頻偵測迴圈。
*   **狀態辨識**:
    *   **State 1**: 偵測到可搶占狀態 -> 點擊「加入」與隨機偏移座標。
    *   **State 2**: 偵測到確認視窗 -> 點擊「確定」。

---

## 3. 網頁戰情室整合 (Web Integration)

**檔案位置**: `push_project/web/菇勇者.html`

為了方便操作，網頁介面已新增以下功能：

### 3.1 一鍵複製指令
*   **操作**: 點擊網頁上的任一車位卡片。
*   **效果**: 自動將該車位的資訊格式化為腳本所需的參數字串並複製到剪貼簿。
*   **格式**: `s{ServerID}-{SpotNum}+{EndTime}` (例如 `s1470-7+14:30:00`)。
*   **應用**: 複製後可直接在終端機貼上執行，無須手動輸入。

### 3.2 吐司訊息 (Toast Notification)
*   點擊複製後，畫面下方會彈出「已複製: ...」的提示，確認操作成功。

---

## 4. 注意事項
*   **執行環境**: 請確保手機/模擬器畫面停留在「跨服車位」的伺服器列表頁面。
*   **OCR 依賴**: 搜尋車位依賴 `img_tools` 的 OCR 識別，請確保解析度設定正確。
*   **中斷執行**: 若需強制停止腳本，請在終端機按 `Ctrl + C`。
