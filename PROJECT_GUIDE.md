# 🍄 菇勇者自動化掛機 - 全域技術指南

本文件定義了整個自動化掛機系統的架構、生命週期以及各個功能模組的協作方式。

---

## 1. 系統入口：多執行緒生命週期

**主入口**: `new_main_before20250514.py`

### 啟動流程：
1.  **設備偵測**: 透過 `get_adb_devices()` 取得所有連接的模擬器或實體手機。
2.  **資源載入**: 載入通用 CNN 模型（用於頁面偵測）與 挖礦專用 CNN 模型。
3.  **多執行緒啟動**: 為每個 IP 位址啟動一個獨立的 `main()` 執行緒。
4.  **Logger 綁定**: 透過 `setup_logger_for_device(ip)` 建立專屬日誌檔案，並使用 `set_thread_logger` 確保該線程內的所有模組呼叫都記錄在正確的檔案中。

---

## 2. 核心迴圈：自動化循環 (The Loop)

每個執行緒進入 `while(1)` 循環，執行以下階段：

### A. 喚醒與解鎖 (Wake-up)
*   **模組**: `utils/wake_up_handler.py`
*   **功能**: 檢查螢幕狀態、執行滑動解鎖、處理多設備間的連線鎖 (Lock)，確保不會多個設備同時競爭資源導致連線崩潰。

### B. 遊戲啟動與狀態檢查
*   **處理**: `game_initialization.py`
*   **邏輯**: 
    1. 檢測是否在遊戲內。
    2. 若否，透過圖示啟動遊戲。
    3. 處理啟動時的各類廣告彈窗 (`handle_game_startup_pages`)。

### C. 任務執行階段 (Task Execution)
系統依據時間或冷卻時間執行以下任務：
1.  **技能管理**: `switch_skill(d)` (針對特定設備切換騙人/實戰技能)。
2.  **副本挑戰**: `new_battle.hell_door(d, ip)` (地獄之門)。
3.  **農場管理**: `farm_manager.farm(d, ip, Cnn_model)`。
4.  **家族任務**: `family_manager.go_to_family()`。
5.  **商店購買**: `Store.buy_store(d, Cnn_model)`。
6.  **挖礦 (Oracle)**: `miner_action.oracle(...)` -> 呼叫 `miner` 模組。
7.  **定時任務**: `_run_periodic_cycle` 處理菇菇武道會等。

### D. 休眠與對齊 (Sleep & Sync)
*   **邏輯**: 執行完一輪任務後，系統會計算下一次喚醒時間。
*   **對齊**: 預設對齊到每小時的 **00~20 分** 區間，模擬真人作息並分散伺服器負載。

---

## 3. 重要任務模組說明

### ⛏️ 挖礦模組 (`miner/`)
*   **核心**: A* 搜尋演算法 (`SmartPlanner`)。
*   **特性**: 具備 7 層視野限制、全收集優先權、道具成本精確評估 (3.0)。
*   **詳見**: `README_NEW_ARCHITECTURE.md`。

### 🏠 家園與家族
*   **農場**: 偵測作物成熟度，自動收割與補種。
*   **家族**: 處理點擊、領取獎勵、參與活動。

### 🛒 商店與資源管理
*   **Json 管理器**: `json_manager.py` 負責記錄各項任務的最後執行時間，避免重複執行。
*   **OCR**: 透過 `ocr_server.py` 提供的高效文字辨識服務讀取數值。

---

## 4. 日誌與除錯架構

### 日誌層級與格式
*   **格式**: `%(asctime)s - %(levelname)s - [%(filename)s:%(lineno)d] %(message)s`
*   **檔案**: 
    *   `logs/{IP}.log`: 記錄整體行為。
    *   `logs/miner_{IP}.log`: 記錄挖礦細節。

### 異常保護
*   **死循環偵測**: 針對高頻點擊區域進行狀態比對。
*   **螢幕重設**: 在實體手機執行後自動重設解析度 (`reset_screen_settings`)。

---

## 5. 開發守則 (Best Practices)
1.  **保持執行緒安全**: 存取全域變數（如 `lock`）時必須使用 `threading.Lock`。
2.  **優先使用座標 + 圖像比對**: 減少對 OCR 的依賴以提升效能。
3.  **任務隔離**: 每個新功能應寫在獨立檔案中，並由 `new_main` 調用。

---
*Created: 2026-01-31*
