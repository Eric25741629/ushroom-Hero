# 截圖資產必要性說明（重要）

更新日期：2026-03-12

## 核心結論

本專案中的畫面截圖、模板圖、偵錯圖**都是必要資產**，不應任意刪除或替換。
此外，**每個截圖在每次流程都必須做即時檢查，不可使用快取結果跳過檢查**。

## 為什麼每個截圖都必要

1. 畫面辨識依賴
- 腳本透過模板匹配/OCR 定位 UI 元素。
- 任一關鍵截圖缺失都可能造成誤判，導致流程停滯或錯點。

2. 多場景適配
- 不同頁面（主城、礦區、戰鬥、活動）需要不同樣板。
- 相同功能在不同解析度或狀態下可能需要多張參考圖。

3. 例外與容錯
- 偵錯截圖可用於回溯當下 UI 狀態。
- 無法還原問題時，調參與修復成本會顯著上升。

4. 模型/規則迭代
- RL、模板閾值、OCR 後處理都可能參照歷史截圖調整。
- 刪除資產會破壞可重現性，影響回歸測試。

## 管理規範

- 不要直接刪除現有截圖。
- 需替換時，先保留舊圖備份並記錄原因。
- 新增截圖應標註用途與來源模組。
- 優先保持命名穩定，避免腳本路徑失效。
- 每次循環都要重新執行截圖判斷（模板/OCR），不得以歷史快取值直接當作當前結果。
- 禁止以「上次命中」或「短期記憶」略過必要畫面檢查。

## 變更建議流程

1. 先確認該圖是否被任何腳本引用
2. 若要替換，先 A/B 驗證辨識成功率
3. 更新後跑最小回歸（啟動、辨識、點擊、切場景）
4. 通過後再正式合併
