---
wave: 1
depends_on: []
autonomous: true
gap_closure: true
files_modified:
  - utils/mumu_control.py
  - utils/emulator_recovery.py
  - tests/test_mumu_control.py
requirements:
  - PHASE-01
must_haves:
  - MuMu control path resolves to MuMuManager.exe fixed path
  - launch/shutdown/restart/show_window/hide_window all execute via MuMuManager path
  - tests cover manager-only environment and prevent regression
---

# Plan 03 - MuMuManager 單一路徑相容修復

<objective>
修補 UAT 缺口：將 MuMu 控制執行檔解析調整為「僅 MuMuManager.exe 路徑為有效控制入口」，確保現場環境可穩定控制模擬器。
</objective>

<tasks>
  <task id="01-03-01">
    <title>固定支援 MuMuManager.exe 路徑</title>
    <details>在 discover/control 初始化流程中加入 `C:\Program Files\Netease\MuMuPlayer\nx_main\MuMuManager.exe` 作為主要可執行檔，避免只找 control.exe。</details>
    <verify automated="true">pytest -q tests/test_mumu_control.py::test_discover_mumu_manager_only_path</verify>
  </task>
  <task id="01-03-02">
    <title>保持控制指令與索引映射行為一致</title>
    <details>確保 launch/shutdown/restart/show_window/hide_window 在 manager 路徑下仍使用既有索引映射邏輯與 timeout/error handling。</details>
    <verify automated="true">pytest -q tests/test_mumu_control.py::test_manager_path_actions_keep_index_mapping</verify>
  </task>
  <task id="01-03-03">
    <title>增加可觀測性與回歸測試</title>
    <details>日誌輸出實際採用 executable path，並加入 manager-only 場景回歸測試，避免後續再退回 control.exe-only。</details>
    <verify automated="true">pytest -q tests/test_mumu_control.py::test_logs_selected_mumu_manager_path</verify>
  </task>
</tasks>

<verification>
- 目標環境（僅 MuMuManager.exe）可成功執行控制命令。
- 日誌可確認採用 MuMuManager.exe 路徑。
- 測試可覆蓋 manager-only 場景並通過。
</verification>
