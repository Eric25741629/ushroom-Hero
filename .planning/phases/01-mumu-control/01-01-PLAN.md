---
wave: 1
depends_on: []
files_modified:
  - config/game_config.py
  - config_manager.py
  - bot_state.py
  - new_main_v2.py
  - control_panel_app.py
  - tests/test_mumu_control.py
  - tests/test_emulator_watchdog.py
autonomous: true
requirements:
  - PHASE-01
must_haves:
  - MuMu control commands are callable by emulator serial
  - Hang detection triggers deterministic restart flow
  - Single emulator failure does not cascade to others
---

# Plan 01 - MuMu Control + Watchdog Core

<objective>
建立 MuMu 指令適配與卡死偵測核心，讓 `emulator-*` 可被穩定重啟。
</objective>

<tasks>
  <task id="01-01-01">
    <title>建立 emulator serial 與 MuMu index 解析器</title>
    <details>新增可測試函式：`emulator-5554 -> 0`, `emulator-5556 -> 1`，支援 config 覆寫。</details>
    <verify automated="true">pytest -q tests/test_mumu_control.py::test_resolve_emulator_index</verify>
  </task>
  <task id="01-01-02">
    <title>封裝 control.exe 指令 API</title>
    <details>封裝 launch/shutdown/restart/show_window/hide_window，包含 timeout、錯誤處理與日誌。</details>
    <verify automated="true">pytest -q tests/test_mumu_control.py::test_build_commands</verify>
  </task>
  <task id="01-01-03">
    <title>實作卡死判定器</title>
    <details>以心跳、畫面 hash、adb timeout 三訊號綜合判定，降低誤判率。</details>
    <verify automated="true">pytest -q tests/test_emulator_watchdog.py::test_hang_detection_rules</verify>
  </task>
</tasks>

<verification>
- MuMu command adapter 在模擬與真機都可執行並回傳可判讀結果。
- Hang 判定可在測試場景中穩定觸發，且非卡死不誤觸。
</verification>
