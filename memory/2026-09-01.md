## Skill Updates

- Learned: New `web_h5` registrations should explicitly set both the legacy `ws_token_open_lamp` flag and nested `ws_token.enabled/open_lamp` flags to `true`; defaults alone do not fully define newly persisted device records.
- Verified: Re-registration must preserve existing per-device WS settings instead of reapplying defaults.
- Workflow note: On this NAS workspace, `git worktree remove` may unregister a worktree while Windows leaves a locked `.pytest_cache` directory behind.
