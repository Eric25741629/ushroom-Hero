# 2026-07-16

## Skill Updates

- Candidate improvement for `finishing-a-development-branch`: when the user chooses an integration option after a later turn/session, re-read the live branch/worktree state before acting. Check whether the known feature commit is already contained in `main` (`git branch --contains <commit>`) and inspect reflog/merge history. If another session already merged and cleaned the branch, skip the duplicate merge and verify the current `main` result instead.
