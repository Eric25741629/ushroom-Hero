## Skill Updates

- Improved: H5 Cocos/OCR migration workflow — `GoodsGetView`/`GoodsGetView2` 的「恭喜獲得」標題是圖片，不能依賴 `cc.Label` 文字或 OCR；應以 active view name 偵測，透過 `uiMgr.close()` 關閉，並輪詢 `node.active` 驗證結果。
- Live verification: 7fe98fc6 關閉 `GoodsGetView` 後，該 view inactive，背景 `ParkingWareHouseView` 保持 active。
