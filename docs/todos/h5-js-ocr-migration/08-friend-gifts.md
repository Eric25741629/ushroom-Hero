# TODO：好友禮物 H5 JavaScript 判斷

## 現況

- `ws_token/couple.py` 已處理伴侶/好友每日禮物，成功時 pipeline 跳過 UI 任務。
- `ws_token/pay_mall.py` 可處理每日免費禮包。
- 舊 UI 任務可能還包含切頁、按鈕、彈窗或切磋，不能假設都被 `couple` 覆蓋。

## 待辦

- [ ] 明確列出「好友每日禮物」舊 UI 函式的所有副作用，區分送禮、免費禮包與切磋。
- [ ] WS 已完成的子任務不得再由 H5 重複執行。
- [ ] 為未被 WS 覆蓋的子任務探查 view、紅點、剩餘次數、可領/可送按鈕與 popup。
- [ ] H5 透過 runtime data 判斷是否可執行，再以 node event 操作並驗證狀態變化。
- [ ] JS 失敗時只 fallback 尚未完成的子任務，不要整組重跑造成重複送禮。
- [ ] ADB 保留現有 UI/OCR 流程。

## 驗收

- [ ] 純 WS 已成功時零 UI、零 OCR。
- [ ] WS 不可用時，H5 正常送禮/領取流程零 OCR。
- [ ] 無伴侶、今日已送、禮包已領及切磋未覆蓋等狀態不會誤標成功。
